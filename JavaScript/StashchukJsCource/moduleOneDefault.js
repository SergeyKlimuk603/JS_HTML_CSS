const pi = 3.14;
export const ex = 2.7;

const sum = (a, b) => {
    return a + b;
}

function sub(a, b) {
    return a - b;
}

export function mult(a, b) {
    return a * b;
}

export default {pi, sum, sub}